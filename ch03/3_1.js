let currentTempC = 22; // 섭씨(~C로 표현)온도, 변수 선언과 초깃값 할당
currentTempC = 22.5; // let 선언 변수는 변경 가능
let targetTempC; // let targetTempC = undefined; 와 같음
let room0,
  room1 = "conference_room_a",
  room2 = "lobby"; // let문 하나에 변수 여러 개 선언, room0은 선언만 하고 값을 할당하지 않아 암시적으로 undefined
const ROOM_TEMP_C = 21.5,
  MAX_TEMP_C = 30; // 상수 선언 및 할당
